# Secrets / local settings — never commit
.env

# App data & configs live outside this folder (C:\MediaStack), but ignore these
# names too in case anyone points them here.
config/
data/

# OS cruft
Thumbs.db
desktop.ini
.DS_Store
